name: plant_care_app
description: A new Flutter project for plant care.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjusted to a common modern Dart SDK range

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.0 # Specify a recent version
  sqflite: ^2.0.0 # Specify a recent version
  intl: ^0.19.0   # Added intl

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0 # Or a recent version

flutter:
  uses-material-design: true
